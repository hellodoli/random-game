export enum Shape {
  CIRCLE,
  SQUARE,
  ROUNDED_SQUARE,
}

export enum Gradient {
  PLAIN,
  RADIAL,
  HORIZONTAL, // left -> right
}

export enum GradientSet {
  DIAMOND,
  GOLD,
  SILVER,
  BRONZE,
  CUSTOM,
}

export enum Type {
  NONE,
  GRADIENT,
}
